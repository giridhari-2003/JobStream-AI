# JobStream-AI
JobStream AI is a multi-agent platform that automates job applications by parsing job descriptions, generating personalized emails, attaching resumes, and sending them via SMTP. It tracks applications in a dashboard, delivers Telegram job alerts, and enables one-tap approval to send AI-crafted outreach at scale.
